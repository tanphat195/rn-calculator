import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  main: {
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
    height: '100%',
  },
  row: {
    width: '100%',
    height: '20%',
    display: 'flex',
    flexDirection: 'row',
  }
})